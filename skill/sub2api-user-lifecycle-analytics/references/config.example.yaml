# Example config for sub2api-user-lifecycle-analytics
#
# Copy to a new file (do NOT commit secrets), then edit values.

url: "https://your-sub2api.example.com"
admin_key: "admin-xxxxxxxxxxxxxxxxxxxxxxxx"

